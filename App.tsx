import React from "react";
import { LandingPage } from "./components/LandingPage";

// Waitlist-Only Application Wrapper
// Facial Analysis features are currently disabled for this deployment.

const App: React.FC = () => {
  return <LandingPage />;
};

export default App;
